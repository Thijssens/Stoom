{# wordt gebruikt in new en edit #}
{{ form_start(form) }}
    <h1>{{ formtitle }}</h1>
    
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    <a href="{{ path('app_game_index') }}">back to list</a>
{{ form_end(form) }}
