{% extends 'base.html.twig' %}

{% block title %}Game{% endblock %}

{% block body %}
    <h1>Game</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Thumbnail</th>
                <td><img src ="{{asset(user.profilepicture) }}"></td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_profile_edit') }}">edit</a>

    {# {{ include('game/_delete_form.html.twig') }} #}
{% endblock %}
